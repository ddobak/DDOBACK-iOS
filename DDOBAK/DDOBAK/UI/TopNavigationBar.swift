//
//  TopNavigationBar.swift
//  DDOBAK
//
//  Created by 이건우 on 7/5/25.
//

import SwiftUI

struct TopNavigationBar: View {
    
    private var shouldShowBackButton: Bool
    private var shouldShowBackButton: Bool
    
    init(
        shouldShowBackButton: Bool,
        shouldShowTitle: Bool,
        shouldShowTrailingButton: Bool,
        trailingIcon: 
    ) {
        
    }
    
    var body: some View {
        HStack {
            Image()
        }
        .frame(height: 42)
        .border(.red)
    }
}

#Preview {
    TopNavigationBar()
}
